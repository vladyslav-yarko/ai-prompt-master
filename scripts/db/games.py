# python -m scripts.db.games

import asyncio

from sqlalchemy.ext.asyncio import AsyncSession

from src.databases import db_session
from src.models import Game

async def fill_games(session: AsyncSession) -> None:
    games = [
        Game(
            title="🎯 Learn Mode",
            shortDescription="Вибери правильний промпт із варіантів",
            description=(
                "У цьому режимі тобі буде запропоновано кілька варіантів промптів. "
                "Твоя задача — обрати той, який найкраще підійде для заданої ситуації. "
                "Навчайся помічати чіткі, структуровані та корисні промпти, уникаючи нечітких або занадто загальних. "
                "Цей режим — чудовий старт для розуміння, як виглядає якісний запит до ШІ."
            ),
            mode="learnMode"
        ),
        Game(
            title="🎨 Creative Mode",
            shortDescription="Напиши промпт для творчої задачі",
            description=(
                "У цьому режимі тобі буде надано ситуацію — наприклад, сюжет, сцену чи опис персонажа. "
                "Твоя задача — придумати та написати такий промпт, який змусить ШІ згенерувати саме цю ситуацію. "
                "Прояви креативність і сформулюй запит так, щоб LLM точно зрозуміла, що від неї хочуть."
            ),
            mode="creativeMode"
        ),
        Game(
            title="💻 Code Mode",
            shortDescription="Напиши промпт для задачі з кодування",
            description=(
                "У цьому режимі тобі буде описано задачу з програмування. "
                "Твоя задача — сформулювати правильний запит для ШІ, який дозволить отримати "
                "очікуваний кодовий результат. Важливо чітко вказати мову, формат і деталі реалізації. "
                "Цей режим розвиває навички створення технічних промптів."
            ),
            mode="codeMode"
        ),
        Game(
            title="🛠️ Anti-prompt Mode",
            shortDescription="Виправ поганий промпт",
            description=(
                "Тут ти отримаєш приклад поганого або неефективного промпта. "
                "Твоя задача — переписати його так, щоб він став точним, зрозумілим і корисним для ШІ. "
                "Навчайся аналізувати недоліки формулювань і покращувати їх. "
                "Цей режим допоможе помічати й уникати типових помилок у запитах."
            ),
            mode="antiPromptMode"
        ),
        Game(
            title="🧩 Prompt Puzzle Mode",
            shortDescription="Збери промпт із частин",
            description=(
                "У цьому режимі ти отримаєш набір слів або фраз, повʼязаних із певним завданням. "
                "Твоя мета — на основі цих фрагментів скласти повноцінний і якісний промпт. "
                "Розвивай креативність, логіку та вміння структурувати запити до ШІ. "
                "Це весела та пізнавальна гра, що прокачує мислення."
            ),
            mode="puzzleMode"
        ),
    ]
    session.add_all(games)


async def main() -> None:
    async for session in db_session():
        tasks = asyncio.gather(
            fill_games(session)
        )
        await tasks
        await session.commit()
    
    
asyncio.run(main())
